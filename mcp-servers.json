{
  "description": "Minimal MCP servers — claude-forge v3.0 (4 servers). Full set: mcp-servers.optional.json",
  "servers": {
    "playwright": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "@playwright/mcp@latest"],
      "env": {},
      "description": "Headless browser automation (navigate, click, fill, screenshot, trace)"
    },
    "context7": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "@upstash/context7-mcp@latest"],
      "env": {},
      "description": "Real-time library documentation (React 19, Next.js 15, etc.) — use BEFORE writing library code"
    },
    "jina-reader": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "jina-mcp-tools"],
      "description": "URL-to-markdown via Jina AI — token-efficient web content (fetch replacement)"
    },
    "chrome-devtools": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "chrome-devtools-mcp@latest"],
      "env": {},
      "description": "Chrome DevTools Protocol — Lighthouse audits, Core Web Vitals (LCP/INP/CLS), memory snapshots, network inspection. Complements playwright with quantitative perf/a11y diagnostics."
    }
  },
  "_notes": {
    "why_4_servers": "Minimal dependency for broad user compatibility. playwright = general browser automation. context7 = live library docs. jina-reader = token-efficient web fetch. chrome-devtools = quantitative perf/a11y audits (complements playwright, heavier but Lighthouse-grade). memory → Claude Code Auto Memory. exa → built-in WebSearch. github → gh CLI. fetch → jina-reader fallback.",
    "add_more": "See mcp-servers.optional.json for 5+ additional servers (memory, exa, github, fetch, time, sequential-thinking). Copy entries into this file and run: ./install.sh --upgrade"
  }
}
